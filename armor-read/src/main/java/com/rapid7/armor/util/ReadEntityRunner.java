package com.rapid7.armor.util;

/**
 * Driver that reads from a set of given columns and reads the entity values as its stored in the column.
 */
public class ReadEntityRunner {

  public static void main(String[] args) {
    boolean asWriter = true;
    if (asWriter) {

    } else {

    }
  }
}
